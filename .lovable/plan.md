

## Changes

1. **Replace "Jane Doe" with "Rahul Sharma"** across all components: HeroSection, Footer, Navbar, and any other references (initials "JD" → "RS", etc.)

2. **Add profile image to Hero Section** — place a circular avatar image next to or above the headline using a placeholder image (e.g., `https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=400&h=400&fit=crop&crop=face` — a professional male portrait). Use the existing Avatar component or a styled `img` tag with a glowing border effect to match the bold theme.

### Files to edit:
- `src/components/HeroSection.tsx` — rename + add avatar image
- `src/components/Footer.tsx` — "JD." → "RS.", "Jane Doe" → "Rahul Sharma"
- `src/components/Navbar.tsx` — update any name/initials references
- `src/components/AboutSection.tsx` — update name if referenced

